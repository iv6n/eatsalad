name: loyverse_api
description: A Dart API Client for the Loyverse API.
version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  http: ^1.6.0
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.16.0
  json_serializable: ^6.14.1
