name: loyverse_api
description: A Dart API Client for the Loyverse API.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  http: ^0.13.0
  json_annotation: ^4.0.0

dev_dependencies:
  build_runner: ^2.0.5
  json_serializable: ^4.0.2
